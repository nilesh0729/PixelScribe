version: "2"
sql:
  - engine: "postgresql"
    queries: "./db/Query"
    schema: "./db/migration"
    gen:
      go:
        package: "db"
        out: "Result"
        emit_json_tags: true
        emit_interface: true
        